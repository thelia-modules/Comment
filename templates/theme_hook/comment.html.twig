{# The module styles its own fields: nothing for the theme to import. #}
<link rel="stylesheet" href="{{ module_asset('Comment', 'assets/comment.css') }}">

{{ component('Comment', {ref: ref, refId: refId}) }}
